import sys, os

class FeatureMatching:
    def __init__(self, config, parameter, ortho):
        self.config    = config
        self.parameter = parameter
        self.ortho     = ortho

    def match_features(self):
        """
        A function for matching features between images.
        """
        pass